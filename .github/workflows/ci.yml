name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        crystal: ["1.21", "latest"]
    steps:
      - uses: actions/checkout@v4

      - name: Install Crystal
        uses: crystal-lang/install-crystal@v1
        with:
          crystal: ${{ matrix.crystal }}

      - name: Cache shards
        uses: actions/cache@v4
        with:
          path: ~/.cache/shards
          key: ${{ runner.os }}-shards-${{ hashFiles('shard.lock') }}
          restore-keys: |
            ${{ runner.os }}-shards-

      - name: Install shards
        run: shards install --frozen

      - name: Check formatting
        run: crystal tool format --check

      - name: Run specs
        run: crystal spec --error-on-warnings

      - name: Build release binary
        run: shards build --release

      - name: Fixture builds
        run: |
          ./bin/plombir new /tmp/ci-site
          cd /tmp/ci-site
          "$GITHUB_WORKSPACE/bin/plombir" build
          "$GITHUB_WORKSPACE/bin/plombir" check

      - name: Benchmark (warn-only)
        run: ./scripts/bench.sh bin/plombir
